{
  "compilerOptions": {
    /* Base Settings */
    "target": "ESNext",                                 /* Use the latest ECMAScript features */
    "module": "ESNext",                                 /* Use ESNext modules for compatibility with Vite */
    "lib": ["DOM", "DOM.Iterable", "ESNext"],           /* Include browser and ESNext APIs */
    "jsx": "react-jsx",                                 /* Use the modern JSX transform for React */
    "esModuleInterop": true,                            /* Allow CommonJS imports with better compatibility */
    "moduleResolution": "Node",                        /* Resolve modules like Node.js */
    "strict": true,                                     /* Enable all strict type-checking options */
    "baseUrl": ".",                                     /* Base directory for resolving paths */
    "paths": { "@/*": ["./src/*"] },                    /* Aliases for cleaner imports */
    "resolveJsonModule": true,                          /* Enable JSON imports */
    "skipLibCheck": true,                               /* Skip type checking of declaration files */
    "forceConsistentCasingInFileNames": true,           /* Enforce case-sensitive imports */

    /* Emit Options */
    "outDir": "./dist",                                 /* Output directory for compiled files */
    "rootDir": ".",                                     /* Source directory */

    /* Additional Features */
    "allowSyntheticDefaultImports": true,              /* Allow default imports even if not specified */
    "isolatedModules": true                             /* Ensure compatibility with Vite's module handling */
  },
  "include": ["src", "vite.config.ts"],                 /* Include source files and Vite config */
  "exclude": ["node_modules", "dist"]                   /* Exclude unnecessary directories */
}
